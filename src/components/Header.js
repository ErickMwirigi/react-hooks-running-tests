import React from "react";

function Header(props) {
      return (
        <h1>
          {/* uncomment the line below to pss this test */}
          {/* hello from the Header! */}
          hello from the Header!
        </h1>
      )
}

export default Header;
